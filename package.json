{
  "name": "quizapp",
  "version": "1.0.0",
  "description": "This is a platform to take quizes.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node backend/server.js",
    "build": "npm install --legacy-peer-deps && npm install --legacy-peer-deps --prefix frontend && npm install --legacy-peer-deps --prefix backend && npm run build --prefix frontend"
  },
  "author": "Srijan Tripathi",
  "license": "ISC",
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
